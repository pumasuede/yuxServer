AUX_SOURCE_DIRECTORY(. DIR_SRCS)
INCLUDE_DIRECTORIES(/usr/local/include)
LINK_DIRECTORIES(/usr/local/lib)
ADD_LIBRARY(Pbout SHARED ${DIR_SRCS})
TARGET_LINK_LIBRARIES(Pbout protobuf)
INSTALL(TARGETS Pbout DESTINATION bin)

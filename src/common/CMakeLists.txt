AUX_SOURCE_DIRECTORY(. DIR_COMMON_SRCS)
INCLUDE_DIRECTORIES(..)
ADD_LIBRARY(Common SHARED ${DIR_COMMON_SRCS})
TARGET_LINK_LIBRARIES(Common protobuf Base)
INSTALL(TARGETS Common LIBRARY DESTINATION bin)

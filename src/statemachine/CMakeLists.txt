AUX_SOURCE_DIRECTORY(. DIR_SRCS)

INCLUDE_DIRECTORIES(include ..)

ADD_LIBRARY(StateMachine SHARED ${DIR_SRCS})
TARGET_LINK_LIBRARIES(StateMachine Pbout)

INSTALL(TARGETS StateMachine DESTINATION bin)
